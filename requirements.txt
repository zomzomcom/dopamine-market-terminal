# ================================================================
# 投资分析应用 - Python 依赖清单
# ================================================================

# Web 框架
Flask==3.1.3

# WSGI 服务器（云端部署使用，本地开发不需要）
gunicorn==23.0.0

# 跨域支持
flask-cors==6.0.5

# HTTP 请求库（获取东方财富行情数据）
requests==2.34.2

# MySQL 驱动（如需连接 MySQL 数据库，取消注释安装）
# PyMySQL==1.1.1
