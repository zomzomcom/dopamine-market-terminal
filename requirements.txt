# ================================================================
# 投资分析应用 - Python 依赖清单
# ================================================================

# Web 框架
Flask==3.1.3

# WSGI 服务器（云端部署使用，本地开发不需要）
gunicorn==23.0.0

# HTTP 请求库（获取东方财富行情数据）
requests==2.34.2

# MySQL 驱动（HeidiSQL 管理数据库用）
PyMySQL==1.1.1

# 密码哈希（werkzeug 内置，用于用户注册/登录）
# Flask 已依赖 werkzeug，无需额外安装
